{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node-bridge"
    }
  ],
  "routes": [
    { "src": "/api/contacts", "dest": "/server.js" },
    { "src": "/api/users", "dest": "/server.js" },
    { "src": "/(.*)", "dest": "/server.js" }
  ]
}
